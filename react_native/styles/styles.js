import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFF',
  },
  input: {height: 40, borderColor: '#eee', borderWidth: 1, marginTop:50, padding: 10}
});

export default styles;
